-- ============================================================
--  PISMO — миграция для пользовательских аватарок
--  Выполнить ОДИН раз в phpMyAdmin (база bdauth, вкладка SQL).
--  Добавляет колонку для хранения картинки аватара пользователя.
--  (Обычный MySQL не поддерживает IF NOT EXISTS для колонок — если
--   колонка уже есть, ошибку "Duplicate column" можно проигнорировать.)
-- ============================================================

ALTER TABLE `users` ADD COLUMN `avatar_data` LONGBLOB NULL DEFAULT NULL;
